
a <- as.keyvalue.classcodes(elixhauser, "icd10se")


test_that("multiplication works", {
  expect_message(
    as.keyvalue.classcodes(elixhauser, "icd10se"),
    "Classification based on: regex_icd10"
  )
  expect_s3_class(a, "keyvalue")
  expect_gte(nrow(a), 1500)
})

